apiVersion: apps/v1
kind: Deployment
metadata:
  name: available-schedules-go
  labels: { app: available-schedules-go }
spec:
  replicas: 2
  selector:
    matchLabels: { app: available-schedules-go }
  template:
    metadata:
      labels: { app: available-schedules-go }
    spec:
      containers:
        - name: available-schedules-go
          image: available-schedules-go:local
          imagePullPolicy: IfNotPresent
          env:
            - name: ERROR_RATE
              value: "0.02"
            - name: EXTRA_LATENCY_MS
              value: "40"
            - name: SERVICE_NAME
              value: "available-schedules"
          ports:
            - containerPort: 8080
          resources:
            requests: { cpu: "75m", memory: "64Mi" }
            limits:   { cpu: "120m", memory: "192Mi" }

